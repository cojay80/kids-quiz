// v1.3.0 Online source config
window.KQ_CONFIG = {
  // 1) Google Sheets (publish to web): put your Sheet ID here, or leave empty to disable
  SHEET_ID: "", // e.g. "1AbCdEf..."
  SHEET_TAB: "questions", // sheet tab name for CSV export
  // 2) Open Trivia DB fallback for General Knowledge
  ENABLE_OPENTDB: true,
  // 3) Unsplash Source (no API key) — use random images by keyword
  ENABLE_UNSPLASH: true
};
